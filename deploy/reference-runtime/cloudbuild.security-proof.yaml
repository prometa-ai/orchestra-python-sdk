steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - deploy/reference-runtime/Dockerfile.security-proof
      - --build-arg
      - VCS_REF=${_VCS_REF}
      - --tag
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
